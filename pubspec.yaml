name: mobile_assginment
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.1
  provider: ^6.1.2
  shared_preferences: ^2.5.5
  sqflite: ^2.4.3
  supabase_flutter: ^2.17.2
  flutter_dotenv: ^6.0.1
  flutter_local_notifications: ^22.3.0
  google_generative_ai: ^0.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
